<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wdelivery.member.dao.MemberEmailDAO">	
	<!-- 이메일 테스트 회원가입 -->
	<insert id="register" parameterType="memberEmailVO">
		insert into mailtest(email, password, nickname) values (#{email},#{password}, #{nickname})
	</insert>
	<!-- 생성된 메일 인증키 업데이트 -->
	<update id="createAuthKey">
		update mailtest set authKey = #{param2} where email = #{param1}
	</update>
	<!-- authstatus 컬럼을 0에서 1로 업데이트 -->
	<update id="updateAuthsyatus">
		update mailtest set authstatus = 1 where email = #{param1}
	</update>
</mapper>